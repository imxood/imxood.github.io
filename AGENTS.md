# 知识库整理 AGENTS 规范

最后更新: 2026-03-07

## 目标

本仓库是一个基于 `mdBook` 的长期知识库. 后续整理工作围绕 2 个目标展开:

1. 构建完整, 可维护, 可检索的文档索引.
2. 整理知识内容, 包括去重, 合并, 拆分, 移动, 重命名, 修复链接.

## 核心原则

- 保留知识优先, 不因整理而丢失有效内容.
- 可检索性优先于表面整洁.
- 语义归档优先于机械平均分布目录.
- 小步整理, 持续可回滚, 禁止一次性大爆炸式重构.

## 仓库边界

- 知识库源文件只在 `src/` 下维护.
- `book/` 是 `mdbook build` 生成产物, 禁止手工编辑.
- 图片, 附件, 示例代码应尽量与所属主题靠近放置.
- 未经确认, 不修改与知识整理无关的代码或资源文件.

## 文档索引规则

- `src/SUMMARY.md` 是全库主索引, 任何新增, 删除, 移动, 重命名文档后都必须同步更新.
- `src/SUMMARY.md` 中不允许保留失效路径.
- 一个正式知识文档原则上应至少在 `src/SUMMARY.md` 中出现一次.
- 重点领域应逐步补充目录级索引, 如 `README.md` 或专题总览页.
- 构建完整索引时, 每篇文档尽量补齐标题, 领域, 路径, 关键词, 内容类型, 整理状态等元信息.

## 目录归类规则

- 优先保持 `src/` 现有一级目录稳定, 不随意新增同级大类.
- 二级及以下目录优先表达子领域, 框架生态, 或专题集合.
- 如果一个文档既能按语言归类, 也能按工具归类, 优先放到读者最可能查找的位置.
- 文档位置不合理时, 应按语义迁移, 而不是只为了让目录树更整齐.

## 知识整理规则

### 去重

出现以下情况时, 视为可疑重复内容:

- 主题相同, 只是标题略有差异.
- 多个文档记录同一工具, API, 或概念的相近内容.
- 一个文档大部分内容已被另一篇更完整文档覆盖.

处理原则:

1. 选一篇作为主文档.
2. 将其他文档中的独有知识合并进去.
3. 更新受影响的索引, 相对链接, 图片引用.
4. 对被替代文档做删除, 改名, 或转为轻量说明页.

主文档优先选择内容更完整, 结构更清晰, 位置更合理, 被引用更多, 或更新时间更近的版本.

### 拆分与合并

- 单篇文档同时覆盖多个弱相关主题, 或教程, 原理, 示例, 排错混杂时, 应考虑拆分.
- 多篇碎片文档共同描述同一主题时, 应考虑合并成一个主文档, 并保留总览入口.
- 拆分后应保留总览页, 子文档聚焦单一主题, 并建立双向链接.

### 移动与重命名

- 移动文档时必须同时检查 `src/SUMMARY.md`, 文档内相对链接, 图片路径, 以及其他文档对它的引用.
- 文件名应主题明确, 避免 `new`, `tmp`, `final`, `副本`, `v2` 等临时命名.
- 对已有大量引用的文件, 除非收益明确, 否则不要轻易重命名.

## 内容清洗规则

整理后的知识文档尽量向以下结构靠拢:

1. 主题或背景.
2. 核心结论.
3. 关键示例.
4. 注意事项或坑点.
5. 相关文档或参考链接.

额外要求:

- 结论优先, 方便快速查阅.
- 示例尽量短小, 可复用.
- 外部资料整理进来时必须先提炼, 不要堆原文链接代替总结.
- 明显过时的内容要标注上下文或适用版本.

## 资源与链接规则

- 图片优先放在离文档最近的 `images/` 目录下.
- 同一主题的图片应尽量放在同一个子目录中.
- 每次整理后必须检查 `src/SUMMARY.md` 链接, 文档相对链接, 图片引用, 以及目录页跳转.
- 禁止留下明知失效的链接.

## 执行流程

后续进行全库整理时, 默认按以下顺序执行:

1. 盘点现有目录与文档.
2. 建立或修复全量索引.
3. 标记重复文档, 空文档, 错位文档, 失效链接.
4. 按专题分批处理结构问题, 去重合并, 主题拆分.
5. 最后统一补充导航, 相关链接, 和整理记录.

执行要求:

- 一次只处理一个明确批次.
- 对批量移动, 合并, 重命名, 必须先给出规则或计划, 再执行.
- 每完成一批整理, 必须说明改了什么, 为什么这样改, 影响了哪些路径, 如何验证.

## 验收标准

整理工作是否合格, 以以下标准衡量:

- `src/SUMMARY.md` 与实际文档一致.
- 重要知识文档都有可追踪入口.
- 重复内容显著减少.
- 错位文档被移动到更合理位置.
- 主要相对链接与图片引用正确.
- `book/` 未被手工污染.
- 如运行 `mdbook build` 失败, 需明确区分是本次引入问题还是仓库原有问题.

## 禁止事项

- 禁止手工编辑 `book/` 下生成文件.
- 禁止只移动文件而不更新索引和相对链接.
- 禁止为了“看起来整齐”而删除尚有价值的知识.
- 禁止把大量不同主题硬塞进单一总文件.
